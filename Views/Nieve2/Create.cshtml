@model ProyectoIdentity.Models.Nieve2

@{
    ViewData["Title"] = "Create";
}

<hr />

<style>
    #container {
        display: flex;
        flex-wrap: wrap; /* Permite que los elementos se envuelvan si no caben en una fila */
        justify-content: center; /* Centra los elementos en el contenedor */
        gap: 20px; /* Espacio entre las imágenes */
    }

    .img-container {
        flex: 1 1 300px; /* Permite que los contenedores de imágenes crezcan o se encojan, pero con un ancho mínimo */
        max-width: 45%; /* Limita el ancho máximo al 45% del contenedor */
        box-sizing: border-box; /* Incluye el padding y border en el tamaño total del elemento */
    }

    .img-fluid {
        width: 91%;
        height: auto;
        display: block;
    }

    #right-side p {
        font-size: 2em; /* Tamaño del texto */
        text-align: justify; /* Justifica el texto */
    }

    .btn-info {
        color: #fff;
        background-color: #17a2b8;
        border-color: #17a2b8;
        border-radius: 25px;
        padding: 20px 40px;
        font-size: 22px;
        transition: all 0.5s;
        margin-top: 30px;
    }

        .btn-info:hover {
            background-color: #138496;
            border-color: #138496;
        }

    .text-right {
        text-align: right;
    }
</style>

<div id="container">
    <div class="img-container">
        <img src="~/images/para.jpg" alt="Imagen de dinosaurio" class="img-fluid" />
    </div>
    <div class="img-container">
        <img src="~/images/walkie.jpg" alt="Imagen de dinosaurio" class="img-fluid" />
    </div>
</div>

<div id="right-side">
    <p>
        Maxime despierta en medio de un bosque, atrapado por las cuerdas de un paracaídas en un árbol, el cual se despierta gracias a su walkie-talkie. Se libera del árbol y ahora necesitas ayudarlo.
    </p>
</div>

<div class="text-right">
    <form method="post">
        <button class="btn btn-info" type="submit" name="accion" value="Página siguiente">Página siguiente</button>
    </form>
</div>
