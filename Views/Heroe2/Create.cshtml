@model ProyectoIdentity.Models.Heroe2

@{
    ViewData["Title"] = "Create";
}

<hr />

<style>
    #container {
        display: flex;
        justify-content: center; /* Centra el contenedor de imágenes */
        gap: 20px; /* Espacio entre las imágenes */
    }

    .img-container {
        max-width: 550px; /* Limita el ancho máximo de las imágenes */
    }

    .img-fluid {
        width: 97%;
        height: auto;
        display: block;
    }

    #right-side p {
        font-size: 2em; /* Tamaño del texto */
        text-align: justify; /* Justifica el texto */
    }

    .btn-info {
        color: #fff;
        background-color: #17a2b8;
        border-color: #17a2b8;
        border-radius: 25px; /* Bordes redondeados */
        padding: 20px 40px; /* Aumenta el padding */
        font-size: 22px; /* Tamaño de la fuente */
        transition: all 0.5s; /* Transición suave */
        margin-top: 30px; /* Margen superior */
    }

        .btn-info:hover {
            background-color: #138496; /* Cambia el color de fondo al pasar el mouse */
            border-color: #138496; /* Cambia el color del borde al pasar el mouse */
        }

    .btn-container {
        display: flex;
        justify-content: flex-end; /* Mueve el botón hacia la derecha */
        margin-top: 30px; /* Margen superior para separar el botón del texto */
    }
</style>

<div id="container">
    <div class="img-container">
        <img src="~/images/portal.jpg" alt="Imagen de portal" class="img-fluid" />
    </div>
    <div class="img-container">
        <img src="~/images/zom.jpeg" alt="Imagen de zombies" class="img-fluid" />
    </div>
</div>

<div id="right-side" class="text-center">
    <p>
        Todo cambió cuando un portal se abrió del cielo y comenzó el ataque zombie.
        Corrieron hacia su carro estacionado para escapar rápidamente, pero decidieron enfrentar al monstruo enorme de una vez por todas.
    </p>
</div>

<div class="btn-container">
    <form method="post">
        <button class="btn btn-info" type="submit" name="accion" value="Página siguiente">Página siguiente</button>
    </form>
</div>
