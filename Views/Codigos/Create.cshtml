@model ProyectoIdentity.Models.Codigo

@{
    ViewData["Title"] = "Jurasico 16.2";
}

<hr />

<style>
    #container {
        display: flex;
        flex-wrap: wrap; /* Permite que los elementos se envuelvan si no caben en una fila */
        justify-content: center; /* Centra el contenedor */
        gap: 20px; /* Espacio entre las imágenes */
    }

    .img-container {
        flex: 1 1 300px; /* Permite que los contenedores de imágenes crezcan o se encojan, pero con un ancho mínimo */
        max-width: 45%; /* Limita el ancho máximo al 45% del contenedor */
        box-sizing: border-box; /* Incluye el padding y border en el tamaño total del elemento */
    }

    .img-fluid {
        width: 91%;
        height: auto;
        display: block;
    }

    #right-side p {
        font-size: 2em; /* Tamaño del texto */
        text-align: justify; /* Justifica el texto */
    }

    .btn-info {
        color: #fff;
        background-color: #17a2b8;
        border-color: #17a2b8;
        border-radius: 25px;
        padding: 20px 40px;
        font-size: 22px;
        transition: all 0.5s;
        margin-top: 30px;
    }

        .btn-info:hover {
            background-color: #138496;
            border-color: #138496;
        }

    .text-right {
        text-align: right; /* Alinea el contenido al lado derecho */
    }

    .text-center {
        text-align: center; /* Centra el texto */
    }
</style>

<div id="container">
    <div class="img-container">
        <img src="~/img/dinoout.jpeg" alt="Imagen de dinosaurio" class="img-fluid" />
    </div>
    <div class="img-container">
        <img src="~/img/bunker.jpg" alt="Imagen de dinosaurio" class="img-fluid" />
    </div>
</div>

<div id="right-side" class="text-center">
    <p>
        ¡Lo lograron! Abrieron el bunker y escaparon del dinosaurio. ¡Qué alivio! Dentro encontraron un lugar acogedor con todo lo que necesitaban para estudiar y dormir.
    </p>
</div>

<div class="text-right">
    <form method="post">
        <button class="btn btn-info" type="submit" name="accion" value="Página siguiente">Página siguiente</button>
    </form>
</div>
