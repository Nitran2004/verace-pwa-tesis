@model ProyectoIdentity.Models.Pedido
@{
    ViewBag.Title = "Confirmación de Pedido";
    Layout = "~/Views/Shared/_Layout.cshtml";
    
    // ✅ OBTENER INFORMACIÓN DE SUCURSAL DESDE TEMPDATA
    string sucursalSeleccionada = TempData["SucursalSeleccionada"]?.ToString();
    string direccionSeleccionada = TempData["DireccionSeleccionada"]?.ToString();
    bool tieneSucursalPersonalizada = !string.IsNullOrEmpty(sucursalSeleccionada);
}

<div class="container">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card mt-4">
                <div class="card-header bg-success text-white text-center">
                    <h3><i class="fas fa-check-circle"></i> ¡Pedido Confirmado!</h3>
                    <p class="mb-0">Tu pedido ha sido recibido exitosamente</p>
                </div>
                <div class="card-body">
                    <div class="row mb-4">
                        <div class="col-md-6">
                            <h5>Información del Pedido</h5>
                            <table class="table table-borderless">
                                <tr>
                                    <td><strong>Número de Pedido:</strong></td>
                                    <td>#@Model.Id.ToString("D6")</td>
                                </tr>
                                <tr>
                                    <td><strong>Fecha:</strong></td>
                                    <td>@Model.Fecha.ToString("dd/MM/yyyy HH:mm")</td>
                                </tr>
                                <tr>
                                    <td><strong>Estado:</strong></td>
                                    <td><span class="badge bg-warning">@Model.Estado</span></td>
                                </tr>
                                <tr>
                                    <td><strong>Total:</strong></td>
                                    <td class="h5 text-success">$@Model.Total</td>
                                </tr>
                            </table>
                        </div>
                        <div class="col-md-6">
                            <h5>Datos de Recolección</h5>
                            <table class="table table-borderless">
                                <tr>
                                    <td><strong>Usuario:</strong></td>
                                    <td>@User.Identity.Name</td>
                                </tr>
                                @if (tieneSucursalPersonalizada)
                                {
                                    <tr>
                                        <td><strong>Sucursal:</strong></td>
                                        <td>
                                            <span class="badge bg-primary">
                                                <i class="fas fa-map-marker-alt"></i> @sucursalSeleccionada
                                            </span>
                                        </td>
                                    </tr>
                                    <tr>
                                        <td><strong>Dirección:</strong></td>
                                        <td>@direccionSeleccionada</td>
                                    </tr>
                                }
                                <tr>
                                    <td><strong>Tipo de Servicio:</strong></td>
                                    <td>
                                        @if (Model.TipoServicio == "Servir aquí")
                                        {
                                            <span class="badge bg-primary">
                                                <i class="fas fa-chair"></i> @Model.TipoServicio
                                            </span>
                                        }
                                        else if (Model.TipoServicio == "Para llevar")
                                        {
                                            <span class="badge bg-success">
                                                <i class="fas fa-shopping-bag"></i> @Model.TipoServicio
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">@Model.TipoServicio</span>
                                        }
                                    </td>
                                </tr>
                                <tr>
                                    <td><strong>Tiempo estimado:</strong></td>
                                    <td>
                                        <span class="badge bg-info">
                                            <i class="fas fa-clock"></i> 15-25 minutos
                                        </span>
                                    </td>
                                </tr>
                            </table>
                        </div>
                    </div>
                    
                    <h5>Detalle de Productos</h5>
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>Producto</th>
                                    <th>Precio Unit.</th>
                                    <th>Cantidad</th>
                                    <th>Subtotal</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var detalle in Model.Detalles)
                                {
                                    <tr>
                                        <td>@detalle.Producto.Nombre</td>
                                        <td>$@detalle.PrecioUnitario</td>
                                        <td>@detalle.Cantidad</td>
                                        <td>$@((detalle.PrecioUnitario * detalle.Cantidad).ToString("F2"))</td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot>
                                <tr class="table-success">
                                    <th colspan="3">Total a Pagar:</th>
                                    <th>$@Model.Total</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    
                    <div class="alert alert-info">
                        <h6><i class="fas fa-info-circle"></i> Información Importante:</h6>
                        <ul class="mb-0">
                            @if (tieneSucursalPersonalizada)
                            {
                                <li><strong>Sucursal seleccionada:</strong> @sucursalSeleccionada</li>
                                <li><strong>Dirección:</strong> @direccionSeleccionada</li>
                                <li>Recoge tu pedido en el punto seleccionado</li>
                                <li>Cuando llegues, menciona el número de tu pedido: <strong>#@Model.Id.ToString("D6")</strong></li>
                            }
                            else if (Model.TipoServicio == "Servir aquí")
                            {
                                <li>Tu mesa te será asignada cuando llegues al restaurante</li>
                                <li>Puedes esperar en el área de recepción</li>
                            }
                            else
                            {
                                <li>Recoge tu pedido en el mostrador</li>
                                <li>Te avisaremos cuando esté listo</li>
                            }
                            <li>Tiempo estimado de preparación: 15-25 minutos</li>
                            <li>Recibirás una notificación cuando esté listo</li>
                            <li>Métodos de pago disponibles: Efectivo, tarjeta, transferencia</li>
                            <li>El pedido se registró con tu usuario: <strong>@User.Identity.Name</strong></li>
                        </ul>
                    </div>
                </div>
                <div class="card-footer text-center">
                    <div class="d-flex justify-content-center gap-3">
                        <a href="@Url.Action("Recomendacion", "MenuRecomendacion")" class="btn btn-warning">
                            <i class="fas fa-utensils"></i> Hacer Nuevo Pedido
                        </a>
                        <a href="@Url.Action("MisPedidos", "PedidoRecomendacion")" class="btn btn-outline-secondary">
                            <i class="fas fa-list"></i> Ver Mis Pedidos
                        </a>
                        <a href="@Url.Action("ListarPedidos", "PedidoRecomendacion")" class="btn btn-outline-primary">
                            <i class="fas fa-cog"></i> Administrar Pedidos
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section styles {
<style>
    .btn-warning {
        color: #fff;
        background-color: #F2A900;
        border-color: #F2A900;
    }

    .btn-warning:hover {
        color: #fff;
        background-color: #e09600;
        border-color: #d48e00;
    }

    .card {
        box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
    }
    
    .table-borderless td {
        border: none;
        padding: 0.25rem 0;
    }
    
    .gap-3 {
        gap: 1rem !important;
    }
    
    .badge {
        font-size: 0.875em;
    }
    
    @@media print {
        .card-footer {
            display: none !important;
        }
    }
</style>
}