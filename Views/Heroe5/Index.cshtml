@model IEnumerable<ProyectoIdentity.Models.Heroe5>

@{
    ViewData["Title"] = "Index";
}

<hr />

<style>
    #container {
        display: flex;
        justify-content: space-between; /* Espacio entre las imágenes */
        flex-wrap: wrap; /* Permite que los elementos se envuelvan si es necesario */
    }

    .img-container {
        flex: 1 1 300px; /* Permite que los contenedores de imágenes crezcan o se encojan */
        max-width: 48%; /* Limita el ancho máximo al 48% del contenedor para dejar espacio entre las imágenes */
        box-sizing: border-box; /* Asegura que el padding y el borde se incluyan en el tamaño total */
    }

        .img-container img {
            width: 86%; /* Ajusta el ancho de la imagen al 100% del contenedor */
            height: auto; /* Mantiene la proporción de la imagen */
            object-fit: cover; /* Mantiene la relación de aspecto y evita distorsión */
        }

    #right-side p {
        font-size: 2em; /* Tamaño del texto del párrafo */
        text-align: justify; /* Justifica el texto */
    }

    .btn-info {
        color: #fff;
        background-color: #17a2b8;
        border-color: #17a2b8;
        border-radius: 25px; /* Bordes redondeados */
        padding: 20px 40px; /* Aumenta el padding del botón */
        font-size: 22px; /* Tamaño del texto en el botón */
        transition: all 0.5s; /* Transición suave para efectos */
        margin-top: 30px; /* Separación del botón del contenido superior */
    }

        .btn-info:hover {
            background-color: #138496; /* Cambia el color de fondo al pasar el mouse */
            border-color: #138496; /* Cambia el color del borde al pasar el mouse */
        }

    .btn-container {
        display: flex;
        justify-content: flex-end; /* Alinea el botón a la derecha */
        margin-top: 30px; /* Margen superior para separación */
    }
</style>

<div id="container">
    <div class="img-container">
        <img src="~/images/van.jpeg" alt="Imagen de carro" />
    </div>
    <div class="img-container" style="text-align: right;">
        <img src="~/images/baila.jpeg" alt="Imagen de baile" />
    </div>
</div>

<div id="right-side">
    <p>
        Al hacerlo juntos, toman el carro hacia la ciudad en busca del pastel. Para entrar a la tienda, Jack baila y distrae a los zombies para que puedan ingresar.
    </p>
</div>

<div class="btn-container">
    <form method="post">
        <button class="btn btn-info" type="submit" name="accion" value="Página siguiente">Página siguiente</button>
    </form>
</div>
